/*Common way of using a function in JS*/
let logMessage = function(message) {
    console.log(message);
}

/*Arrow Function in TS*/
let Log = (message) => {
    console.log(message);
}

/* 
If the function is just one line, we need not include parenthesis.
If the parameter is just one, we need not use parenthesis to the param list too.
*/

/*
This is similar to lamda function in Java
*/